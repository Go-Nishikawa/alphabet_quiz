# 略語クイズアプリ 仕様書 (v1 / MVP)

## 1. コンセプト
アルファベットの略語(GDP, CPU, WHO など)について、「正式名称は?」「特定の文字は何の略?」を問うクイズアプリ。
例:「GDPのGは何の略?」→ Gross

## 2. プラットフォーム / 技術構成
- Webアプリ(ブラウザで動作)
- React + Vite
- 状態管理は React の useState / useContext で十分(Redux等は不要)
- バックエンドなし。問題データは静的JSONとして同梱
- スコア・履歴は localStorage に保存(v1ではサーバー同期なし)
- レスポンシブ対応(スマホのブラウザでも操作できるように)

## 3. 画面構成

### 3-1. トップ画面
- カテゴリ選択(カード形式、複数選択 or 単一選択でクイズ開始)
- 出題モード選択(選択式 / タイピング式)をトグルで切り替え
- 「クイズを始める」ボタン

### 3-2. 出題画面
- 現在の問題番号 / 全問題数
- 略語(例: GDP)を大きく表示
- 出題パターンは2種類、ランダムに混在させる:
  - (a) 正式名称を問う:「GDPの正式名称は?」
  - (b) 1文字を問う:「GDPのGは何の略?」
- 選択式モード: 4択のボタン
- タイピング式モード: テキスト入力欄 + 回答ボタン
- 回答後、即座に正誤判定 + 簡単な解説(略語の意味)を表示
- 「次の問題へ」ボタン

### 3-3. 結果画面
- 正解数 / 正答率
- 間違えた問題の一覧(略語・正解・自分の回答)
- 「もう一度」「トップに戻る」ボタン

## 4. カテゴリ(初期セット)
最初は以下4カテゴリ、各20問程度でスタート。後から追加しやすいデータ構造にする。

| カテゴリID | 名称 | 例 |
|---|---|---|
| business | ビジネス・経済 | GDP, KPI, ROI, B2B |
| tech | IT・テクノロジー | CPU, RAM, API, HTTP |
| international | 国際機関・政治 | WHO, NATO, EU, UN |
| slang | ネットスラング | LOL, FYI, ASAP, BTW |

## 5. データ構造(問題データ)

```json
{
  "id": "gdp",
  "category": "business",
  "abbreviation": "GDP",
  "fullForm": "Gross Domestic Product",
  "meaningJa": "国内総生産",
  "letterHints": [
    { "letter": "G", "word": "Gross" },
    { "letter": "D", "word": "Domestic" },
    { "letter": "P", "word": "Product" }
  ],
  "difficulty": "easy"
}
```

- `letterHints` を持たせることで、「1文字だけ問う」出題と「正式名称を丸ごと問う」出題を同じデータから生成できる
- 4択の選択式では、同カテゴリ内の他の問題からダミー選択肢(誤答)を自動生成する
- タイピング式の採点は大文字小文字を無視し、前後の空白をtrimして比較する(スペルの表記ゆれ許容は v2以降で検討)

ファイル配置イメージ: `src/data/questions/business.json`, `src/data/questions/tech.json` ... のようにカテゴリごとに分割

## 6. スコアリング
- 正解ごとに +1、連続正解数(コンボ)を画面に表示
- 結果画面で正答率(%)を表示
- v1ではランキングやユーザー登録は無し(localStorageに直近のプレイ履歴を保持する程度)

## 7. MVPスコープ
含める:
- 上記4カテゴリ、選択式/タイピング式の切り替え、結果画面、正誤解説

含めない(v2以降で検討):
- ユーザー登録・ログイン
- オンラインランキング
- タイマーモード(制限時間つき)
- 間違えた問題だけを再挑戦する復習モード
- 問題データのAI自動生成・追加
- 表記ゆれに寛容なタイピング判定(レーベンシュタイン距離など)

## 8. 今後の拡張候補(メモ)
- カテゴリの追加(医療・科学、スポーツ、時事ニュースなど)
- 難易度別モード
- 間違えた問題の復習リスト機能
- SNSシェア機能(結果をシェア)
